import { Tracker } from '@/components/Tracker'

export default function genomeView(gene: string, data: string[]) {
    return (
        <main style={{ display: "flex", height: "100vh", justifyContent: "center", alignItems: "center" }}>
            <h1>Genome View</h1>
            <p>Hi</p>
        </main>
    );
}